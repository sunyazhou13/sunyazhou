I"�<p><img src="/assets/images/20220414BranchManage/git.avif" alt="git" /></p>

<h1 id="preface">Preface</h1>

<p>This article carries strong personal sentiment. If it makes you uncomfortable, please close it as soon as possible. This article is only for personal study records. Reprinting or sharing within the scope of the license is also welcome. Please respect the copyright and keep the original link. Thank you for your understanding and cooperation. If you find this site helpful, you can subscribe via RSS. Thanks for your support!</p>

<h2 id="a-branch-management-model-suitable-for-client-development">A Branch Management Model Suitable for Client Development</h2>

<p><img src="/assets/images/20220414BranchManage/BranchGuide.avif" alt="Branch Guide" /></p>

<p>First, <code class="language-plaintext highlighter-rouge">DEV</code> represents the development branch<br />
First, <code class="language-plaintext highlighter-rouge">RB</code> represents the release branch</p>

<blockquote>
  <p>Note: this naming and approach borrow from the internal branch management previously used at Kuaishou.</p>
</blockquote>

<p>When development is complete and the build is submitted for testing, the next version’s RB and DEV branches are automatically created. This cycle repeats, enabling iterative management.</p>

<h3 id="questions-people-care-about">Questions People Care About</h3>

<h4 id="how-to-handle-rb-code-changes-that-dev-wants-to-use">How to handle RB code changes that DEV wants to use?</h4>

<p>After the RB branch is modified, how do you sync it to the DEV branch? Under normal development, given code review, you can raise a Merge Request from RB to DEV. e.g., <code class="language-plaintext highlighter-rouge">RB1.6.0</code> Merge to <code class="language-plaintext highlighter-rouge">DEV1.6.1</code>.</p>

<p>If it’s just a few simple commits, I suggest manually executing <code class="language-plaintext highlighter-rouge">git cherry-pick commitIDXXX</code> to bring the RB commits to the DEV branch. (That is, you need to switch to the DEVxxx branch, then run <code class="language-plaintext highlighter-rouge">git cherry-pick commitIDXXX</code>.)</p>

<h4 id="how-to-handle-the-rb-branch-after-release">How to handle the RB branch after release?</h4>

<p>In theory, after release, 2 things need to be done:</p>

<ol>
  <li>Merge into <code class="language-plaintext highlighter-rouge">master</code> and then tag.</li>
  <li>Delete the <code class="language-plaintext highlighter-rouge">RBxxx</code> branch.</li>
</ol>

<blockquote>
  <p>If after the above operations, DEV wants to use the RB changes but the RB branch has been deleted, you can directly merge code from master to DEV.</p>
</blockquote>

<h4 id="what-is-the-purpose-of-the-rb-branch">What is the purpose of the RB branch?</h4>

<ol>
  <li>Only accepts bug fixes</li>
  <li>Doesn’t accept feature development; can’t merge from DEV</li>
</ol>

<blockquote>
  <p>Note: RB cannot merge DEV; only DEV can merge into RB!<br />
Note: RB cannot merge DEV; only DEV can merge into RB!<br />
Note: RB cannot merge DEV; only DEV can merge into RB!</p>
</blockquote>

<h2 id="summary">Summary</h2>

<p>Every team has its own way of managing branches. No approach is inherently better than another; it’s only about which management style fits better.</p>

<p>–
This article was written by<br />
sunyazhou <br />
[CN] sunyazhou.com <br />
This material is protected by copyright</p>

:ET