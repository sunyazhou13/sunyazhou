# 2026-08-22

- 博客多语言建文流程：确认 `bundle exec jekyll post` 来自 jekyll-compose 0.12.0，其源码中路径写死为 `_posts/`，不支持语言子目录。多语言方案为 jekyll-polyglot（`lang_from_path: true`，`_posts/` = 中文默认，`_posts/en/` = 英文）。
- 新增 `tools/newpost.sh`：包装 jekyll-compose，用法 `bash tools/newpost.sh "Title"`（默认双语：`_posts/en/` 存在则同时建英文版，不存在只建中文），也支持 `zh|en|both "Title"` 显式模式（en/both 模式下 en 目录缺失会自动 mkdir）。坑：jekyll 输出含 ANSI 颜色码（`\e[36m`），解析时需 sed 剥离；zsh 下无匹配 glob 会中断命令链，测试时注意。
- 新增正文模板机制（类 hexo scaffolds）：`tools/scaffolds/post.md`（中文前言模板）、`tools/scaffolds/post.en.md`（英文 Preface），newpost.sh 创建后自动追加到 front matter 之后。标准前言文案取自 123 篇文章的统一版本。
- `_config.yml` 新增 `jekyll_compose.default_front_matter.posts`（注意键是复数 `posts`，写 `post` 不生效——jekyll-compose 源码里写死传 "posts"），注入 categories/tags/typora-root-url，后经用户确认又加了 math: true / mermaid: true（用户知悉每页会加载 MathJax/mermaid.js 的开销，存量文章不受影响）。jekyll-compose 不支持正文模板（`FileInfo#content` 只输出 front matter），正文模板只能脚本层实现。
- 用 newpost.sh 创建首篇双语文章：`_posts/2026-08-22-jekyllpostscaffoldmultilang.md`（中文）+ `_posts/en/2026-08-22-jekyllpostscaffoldmultilang.md`（英文），记录博客升级过程（类 hexo scaffolds + 多语言创建）。已跑完整 jekyll build 验证无报错，双语 URL `/2026/08/jekyllpostscaffoldmultilang` 与 `/en/2026/08/...` 均正常生成。注意：macOS 文件系统大小写不敏感，脚本生成的文件名实际为全小写。
- 第二篇双语文章：`2026-08-22-jekyllcomposecompleteguide.md`（jekyll-compose 完全指南：7 个命令、公共选项、两个配置项、源码级行为细节、2019 年停更现状），文内用相对路径 `/2026/08/jekyllpostscaffoldmultilang` 互链前一篇，构建验证通过。不用公式的文章手动删掉自动注入的 math/mermaid 行。用户指出 Hexo 迁移至今是"快九年"不是"三年"，两篇均已修正。
- 用户报构建错误 `YAML Exception reading .container/categories/--ios.html`。根因链：块状 front matter `categories:\n- iOS`（jekyll-compose/newpost.sh 原始产物格式）→ create_pages.sh 的 `yq e ".categories | .."` 把数组节点行 `- iOS` 也当分类名 → sed 空格转横杠后变 `--iOS` 生成 `--ios.html` → 页面内 `title: - iOS` 是非法 YAML。修复两处：① `_scripts/sh/create_pages.sh` 改用 `yq e '.categories | .. | select(type == "!!str")'`（本机 yq 不支持 if/then/else，type 返回 `!!seq`/`!!null` 格式），顺带消灭了历史垃圾页 `---.html`/`null.html`/`[ios,-jekyll].html`（全量对比：21 个正经分类页无丢失，20 个垃圾页清除）；② `tools/newpost.sh` 新增 ruby 归一化（块状数组→行内 `categories: [iOS]`，与全站风格一致）。端到端复现踩雷场景构建 0 异常。注意：_site 里还有用户那次构建留下的垃圾目录 `categories/--ios`，下次 build.sh 会自动清掉。
- 配图收尾：两篇文章按 `assets/images/YYYYMMDD+slug首字母大写` 惯例建图目录（`20260822Jekyllpostscaffoldmultilang`/`20260822Jekyllcomposecompleteguide`），ImageGen 生成 flat illustration 封面。第一轮图带 AI 水印+乱码文字，第二轮加强 no text/no watermark 约束重新生成后删除第一轮旧图，重命名 `cover.png`。插入位置遵循全站风格：front matter 之后、`# 前言` 之前 `![cover](/assets/images/.../cover.png)`。构建验证 4 个页面（中英各 2）均正确引用。
- 配图目录改为可选参数：newpost.sh 新增 `--img`/`-i` 全局 flag（可放任意参数位置，默认**不创建**配图目录），经用户确认。沙箱测试覆盖：默认不建目录、flag 在前/在后、zh 模式组合。顺带修两个 bug：① normalize_front_matter 的块状数组正则要求每行有尾随 \n，key 为 front matter 末行时不生效，改为 `(?:\n|$)`；② usage() 会把 shebang 行漏进输出，加 `grep -v '^#!'`。
- 配图转 AVIF（全站图片惯例）：两张 cover.png 用 managed venv 的 Pillow 12.2.0（原生支持 AVIF 编码）转成 cover.avif，quality=85（与站点同类 banner.avif ~38KB 档位一致），1216x832 flat illustration 压到 39KB/18KB。本机无 avifenc/ffmpeg/ImageMagick，Pillow 是唯一路径。已删源 PNG、更新 4 篇文章引用为 .avif、构建验证通过。
- 文章同步 `--img` 参数：第一篇双语文章（jekyllpostscaffoldmultilang 中英两版）的"使用方法"章节补充了 `--img`/`-i` 用法示例和配图目录命名规则说明（日期无分隔符+slug 首字母大写，默认不创建）。第二篇 compose 指南不涉及 newpost.sh，无需改。构建验证通过。
- 配图去水印：ImageGen 生成的两张图带右下角“AI生成 WORKBUDDY”水印。Pillow 无法直接消除，重生成也难保证无水印，故采用底部裁切 72px（水印在底部纯色/留白区域，主体内容未受损）。裁剪预览确认干净后，用 1216x760 版本以 q85 重新覆盖 cover.avif，构建验证通过。
- 新增博客「工具」模块（用户提出的独立工具 tab 架构，已实现）：注册制设计，`_data/tools.yml` 为工具注册表（每工具 enabled 开关），`tabs/tools.md` 落地页按注册表渲染卡片，`tools/<id>.md` 为工具页，`assets/tools/<id>/` 存放该工具全部 JS/CSS/WASM。三级开关：L1 tabs.yml 条目 hidden:true 软隐藏；L2 `_config.yml tools.enabled:false` + `_plugins/tools-switch.rb`（post_read 钩子 reject pages，构建产物零残留）；L3 删目录。首个工具：图片转 AVIF（纯前端，canvas.toBlob('image/avif') 原生探测优先（Chrome/Edge 121+），回退自托管 jSquash 单线程 WASM libaom；fflate 打 ZIP；GIF/WebP 动图检测取首帧；重名自动加序号；WASM 串行防炸内存）。
- 关键坑记录：① Chirpy `_config.yml` 原本 `exclude: [tools]` 会把工具页面整个排除，已改为 glob 精确排除 `tools/*.sh`、`tools/*.py`、`tools/scaffolds`（保留主题开发脚本排除）；② polyglot parallel_localization 多进程并发首建 `.jekyll-cache` 有竞态（mkdir EINVAL），重跑即好；③ jSquash 2.1.1 的 encode wrapper 依赖 wasm-feature-detect 包，自托管应绕过 wrapper 直接用 `codec/enc/avif_enc.js`（embind 运行时注册 module.encode，wasm 靠 import.meta.url 同目录解析）；④ 主题暗色模式属性是 `html[mode=dark]`（jQuery attr('mode')），非 data-theme。改动侵入面：sidebar.html +hidden 守卫（约5行）、tabs.yml/tabs_en.yml 各+3行、_config.yml +tools块和sitemap排除。
- 线上图片不显示排查（部署架构）：真实链路 = 源仓库 → 根目录 `build.sh`（构建+同步+推送）→ 部署仓库 sunyazhou13/sunyazhou13.github.io（本地克隆 `~/Documents/sunyazhou13.github.io`）→ 其 `.github/workflows/static.yml`（Deploy static content to Pages，上传整个仓库）→ 线上。源仓库的 `jekyll.yml` Actions 是死链路（deploy-pages 404，从未成功过），已删除（commit 20dd31ea）。
- 更正：根目录 `build.sh` 本身就是完整部署脚本（构建 → rm _site 垃圾文件(config.codekit3/build.sh/Brewfile/Brewfile.lock.json) → `rsync -auv _site/ ../sunyazhou13.github.io`（增量不带 --delete，所以 static.yml 等历史文件得以保留）→ 进部署仓库跑 push.sh 交互式推送）。本次图片没上线根因即：15:40 只跑了内层 tools/build.sh（纯构建），没跑根 build.sh。已补清理部署仓库中误同步的 4 个垃圾文件（commit ab3ee9e9）。以后部署一律跑根目录 `sh build.sh`。

- ⚠️ 同步 _site 的教训：rsync `--delete` 会把部署仓库的 `.github/workflows/static.yml`（部署命脉）和 `.gitignore` 删掉，导致推送不触发部署。以后同步必须 `rsync -a --delete --exclude '.git' --exclude '.github'`（或删后从旧提交恢复）。本次踩雷后已从 4e1b943c 恢复并重新推送（141b2679 → 80d72321），线上已验证 4 个页面（中英各 2）cover.avif 全部 200、去水印版（34623/18227 字节）。顺带清理：about/ 占位页（内容是 Jekyll 默认模板文案，真实关于页在 /tabs/about/）、历史 index.html.md 副本、垃圾分类页。tools 功能（image-to-avif 工具页）经用户确认一起上线。
